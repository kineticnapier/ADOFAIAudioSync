# Changelog

このプロジェクトの主な変更を記録します。

## [1.0.0] - 2026-08-01

### Added

- 利用者向けの機能説明を日本語で拡充
- インストール、ショートカット、ビルド、既知の制限を含む英語READMEを追加

### Changed

- 正式版としてアセンブリおよび配布パッケージのバージョンを`1.0.0`へ更新
- 実行時の同期処理はv0.9.24から変更なし

### Verified

- 添付ゲームDLLでHarmonyパッチ対象のメソッドとフィールドが存在することを確認
- `scrController.Scrub`内の`crotchetAtStart`直接参照が想定どおり2件で、高BPM助走パッチの安全条件が成立することを確認
- プロジェクト登録、版番号参照、旧Play再実行コードの不在を静的確認

## [0.9.24] - 2026-08-01

### Fixed

- 高BPMカウントダウン折りたたみが`crotchetAtStart`全体を書き換え、本体`Scrub`の開始時刻下限まで拡大していた問題を修正
- 3200 BPMの床77で`crotchetAtStart`が`0.300秒`から`4.800秒`になり、`4.800 × 4 = 19.200秒`へシークして床786前後まで飛ぶ経路を除去
- 惑星の助走時間に使う最初の`crotchetAtStart`参照だけへ折りたたみ倍率を適用し、開始時刻下限と音声同期には本来値を維持
- 対応ゲーム版で助走参照を特定できなかった場合は、床位置を壊さず折りたたみだけを自動停止

### Changed

- カウントダウン診断ログへ本来の`conductorCrotchet`、助走専用の`leadInCrotchet`、本体の`minimumScrubTime`を個別出力
- v0.9.23で除去した`scnGame.Play`二重通知対策は維持

## [0.9.23] - 2026-08-01

### Fixed

- `scnGame.Play`をHarmony Prefixで抑止して`scnEditor.Play`のPostfixから再実行する開始ゲートを廃止
- 抑止された呼び出しと再実行された呼び出しの両方で他ModのHarmony Postfixが発火し、コンボ・checkpoint状態が二重初期化される問題を修正
- 3200 BPM区間の床77付近から開始した際、コンボが789で始まる、無音になる、開始直後に失敗する症状の原因経路を除去

### Changed

- `scnGame.Play`パッチを、引数・戻り値・実行回数を変えない診断観測専用へ変更
- 途中再生handshake、DSP予約、デコーダ予熱、高BPMカウントダウン折りたたみは変更なし
- 使用されなくなった開始ゲート設定と`Ctrl+F8`操作をUIから削除（旧`Settings.xml`の項目は移行用に保持）

## [0.9.22] - 2026-07-31

### Fixed

- Windowsで生成したZIPのエントリ名に`\\`が入ると、UMMの更新インストールが`Substring`例外で失敗する問題を修正
- ZIPエントリを常に`ADOFAIAudioSync/...`形式で生成し、`\\`、重複、余計なエントリをビルド後に拒否
- 高速再開時に`scnGame.Play`だけを後のフレームへ遅延し、再読込後の床配列とcheckpointが食い違う問題を修正
- 開始位置が存在しない後方の床へ飛ぶ、音が始まらない、開始直後に失敗する症状を修正
- ADOFAI本体が指定したcheckpointを譜面の長さで剰余化せず、そのまま維持

### Changed

- 停止時の予約解除・AudioSource停止は維持し、`ReloadAssets`後の`scnGame.Play`は本体と同じ`scnEditor.Play`呼び出し内で開始
- 古い譜面の床数を前提にした床番号復元は行わない
- 途中再生handshake、DSP予約、デコーダ予熱、カウントダウン折りたたみは変更なし

## [0.9.21] - 2026-07-31

### Changed

- UMM用ZIPのルートを`ADOFAIAudioSync/`へ修正
- 配布ZIPをDLLと`Info.json`だけにし、PDBはローカル配置時だけ使用
- ビルド後にZIPの必須ファイルと余計なファイルを自動検証
- 実行時の版表示をアセンブリ情報から取得するよう一元化
- READMEへ導入方法、ショートカット、対応環境、既知の制限を追加
- v1.0.0向け回帰チェックリストとMIT Licenseを追加
- 使用されていなかったライフサイクル引数を削除

### Runtime

- 同期・キャッシュ・カウントダウン・タップ補正の挙動変更なし

## [0.9.20] - 2026-07-31

### Fixed

- BPM補正後に不要な復元SetSpeedを追加する処理を削除
- `levelPath`を含む譜面識別で、別譜面への計測結果持ち越しを防止
- 簡易表示を予約残差とplayhead補正の個別表示へ修正
- 音源末端ではゲーム本体のScrub処理へ安全にフォールバック
- OGGキャッシュ消去と容量上限をゲーム本体の参照状態にも反映
- 途中再生失敗時のゲーム本体フォールバックを追加
- ショートカット設定の保存、タップ時pitch取得、Harmonyパッチの個別失敗を修正

### Removed

- 旧ドリフト監視、自動補正、DSPプローブ音、CSV出力
- 診断用の初回再生プリウォーム
- 実行経路のない旧設定と重複UI

## [0.9.19] - 2026-07-31

### Fixed

- 音声更新間の`delta=0`フレームで開始確認回数が消える問題を修正
- 異なる音声更新を2回観測した時点で途中再生を確定

## [0.9.18] - 2026-07-31

### Changed

- OGG表示を現在の利用状態（`LOAD`、`RAM`、`HIT`、`OFF`）へ変更
- 初回DSP予約前の無音デコーダ予熱と完全な失敗診断ログを追加
